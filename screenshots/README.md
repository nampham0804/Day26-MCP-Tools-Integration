# Minh Chứng Hoàn Thành Lab 04 — Weather Agent

Thư mục này chứa các ảnh chụp màn hình minh chứng cho việc đã thiết lập, cấu hình và chạy thành công Lab 04:

1. **`verify_setup_success.png`**: Ảnh chụp màn hình kết quả chạy script xác minh `verify_setup.py` đạt trạng thái `✅ All checks passed!`.
2. **`agent_chat_demo.png`**: Ảnh chụp màn hình giao diện chat thực tế với Weather Agent trên trình duyệt `http://localhost:8000`, hiển thị kết quả gọi MCP Tool lấy dữ liệu thời tiết thực tế.

